{
  "name": "@hermes/codex-claude-shim",
  "version": "0.1.0",
  "private": true,
  "description": "Codex app-server compatibility transport for the Claude Agent SDK",
  "type": "module",
  "bin": {
    "codex": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "check": "npm test",
    "fix": "echo 'nothing to fix'",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.207",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "22.20.0",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
